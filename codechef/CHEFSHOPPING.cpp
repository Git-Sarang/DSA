#include <iostream>
#include <vector>
#include <unordered_map>
using namespace std;

int main() {
    int cases;
    cin>>cases;
    
    while(cases--) {
        int n;
        cin>>n;
        vector<int> l(n+1);
        vector<int> r(n+1);
        for(int i=1; i<=n; i++) cin>>l[i];
        for(int i=1; i<=n; i++) cin>>r[i];

        
    }
}